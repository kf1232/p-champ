/**
 * Portal feature — landing at `/` (“Fink Social” hub linking to other areas).
 *
 * See `components/README.md` for layout conventions vs `commons` and other features.
 */
export {
  PORTAL_DESCRIPTION,
  PORTAL_TITLE,
} from "./portalHomeCopy";
export { PortalHomeScreen } from "./PortalHomeScreen";
