import WowGroupDetailScreen, {
  generateMetadata,
  generateStaticParams,
} from "@/components/wow/WowGroupDetailScreen";

export const dynamic = "force-dynamic";

export default WowGroupDetailScreen;
export { generateMetadata, generateStaticParams };
