import { CLIENT_ERROR } from "../constants.js";

export { CLIENT_ERROR };

export const QUERY_PARAMS = Object.freeze({
  REGION: "region",
});

export const REQUIRED_QUERY_PARAMS = Object.freeze([QUERY_PARAMS.REGION]);
